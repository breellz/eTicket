import { createEvent, getEventById, bookEvent, isEventBookedByUser, addToWaitList } from "./event.service";

export default {
  createEvent,
  getEventById,
  bookEvent,
  isEventBookedByUser,
  addToWaitList
}